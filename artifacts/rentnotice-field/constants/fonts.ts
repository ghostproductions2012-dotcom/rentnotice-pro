/**
 * Font family names for Plus Jakarta Sans (loaded in app/_layout.tsx).
 * Use these everywhere instead of the `fontWeight` style prop so the
 * custom typeface renders consistently across platforms.
 */
export const fonts = {
  regular: "PlusJakartaSans_400Regular",
  medium: "PlusJakartaSans_500Medium",
  semibold: "PlusJakartaSans_600SemiBold",
  bold: "PlusJakartaSans_700Bold",
} as const;
