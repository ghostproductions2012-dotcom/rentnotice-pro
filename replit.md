# RentNotice Pro — Notice Suite

## Overview
Offline-first, desktop-ready eviction-notice preparation suite for California property managers. Prepares CA 3-Day Notices (and other CA notice types), imports rent ledgers from PM software exports, performs rent-only demand calculations, runs two-level legal validation (warnings vs blockers), computes court-day deadlines with CA judicial holidays, and generates finished PDF document packets — all client-side with no server dependency for core features. NOT legal advice; all output gated behind a prominent legal disclaimer and attorney-review flags for non-CA templates.

## Architecture
- **Web app**: `artifacts/rentnotice-pro` (React + Vite, previewPath `/`), package `@workspace/rentnotice-pro`.
- **Local-first data**: sql.js (SQLite WASM) persisted to IndexedDB (`src/lib/db/`), versioned migrations, seeded demo data, JSON + SQLite backup export/import.
- **Shared contract** (do not change without updating all consumers): `src/lib/types.ts` (domain types; money = integer cents; dates = ISO strings), `src/lib/api/services.ts` (AppServices interface + registry), `src/lib/api/hooks.ts` (TanStack Query hooks).
- **Service implementation**: `src/lib/api/impl.ts` — registered via `registerServicesFactory` at module load (imported in `main.tsx`).
- **Engines** (pure functions): `src/lib/engine/` — classification, rent-only calculation, validation, CA judicial holidays, deadline counting, notice-type + 50-state rules.
- **Import**: `src/lib/import/` — CSV (PapaParse), Excel (SheetJS), text PDF (pdfjs-dist), OCR (tesseract.js), column auto-mapping, PM vendor presets (AppFolio/Buildium/Yardi/Propertyware/Rent Manager).
- **Documents**: `src/lib/documents/` — pdf-lib generators for all document kinds, DRAFT watermark, merge-field templates, packet assembly. Template data in `src/lib/templates-data/` (CA full templates; 50-state attorney-review-gated).
- **Frontend**: `src/pages/`, `src/components/`, wouter routing. Login form is labeled "Email" + "Password" (no pre-auth user list; generic "Invalid email or password" error on failure), but auth still accepts legacy usernames and numeric PINs unchanged. Demo accounts: `arivera`/1234 (admin), `mlee`/2345 (manager), `jchen`/3456 (staff), emails `@goldenstatepm.com`. Usernames/emails live in `users` table (migration v3 backfills first-initial+lastname for pre-existing DBs).
- **Licensing / activation** (desktop side; cloud service is Task #14's separate product): first-run screen offers "Explore the demo" (seeds demo data, `workspace_mode=demo`) or "Activate with a license key" (wipes local DB, provisions company + user directory from the cloud, signs the member in). Migration v4 adds `activation` table, `users.cloud_user_id`, `app_meta.workspace_mode` (backfilled to `demo` for pre-existing DBs). Adapter interface in `src/lib/licensing/` (`types.ts`, `gate.ts` pure grace-period logic, `mock.ts` dev adapter; HTTP adapter pending Task #14's API). Cloud is source of truth for identity/roles; PINs are sha256-cached locally on first online login per device so later logins work offline; tenant/notice data never leaves the device. Launch-time + manual ("Sync now" in Settings) re-verification; paused/cancelled or grace-period expiry (default 14 days offline) makes the workspace **view-only** — reads work, all writes throw (enforced in `requirePermission`). Mock keys: `RNP-TEST-ACTIVE` / `RNP-TEST-PAUSED` / `RNP-TEST-CANCELLED` / `RNP-TEST-OFFLINE` (company "Golden State Property Management, Inc.", directory = the three demo accounts above). Mock adapter is strictly dev-only (`import.meta.env.DEV`); prod builds throw `LicensingUnavailableError` until the HTTP adapter exists. Dev/test localStorage overrides: `rentnotice-pro:mock-network="offline"`, `rentnotice-pro:mock-status="paused"|"cancelled"|"active"` (affects status checks on next sync). **HTTP adapter hard requirement**: map ALL transport failures (fetch TypeError, timeout, 5xx) to `LicensingUnavailableError` — callers treat it as "offline, keep cached state"; any other error surfaces as a real failure. Task #14 must also expose a per-member credential-verification endpoint and `graceDays` in its license payload.
- **Desktop packaging**: `artifacts/rentnotice-pro/src-tauri/` (Tauri v2, config-only) + `.github/workflows/release.yml` (installers) and `ci.yml`. See `artifacts/rentnotice-pro/BUILDING.md`.
- **Licensing website**: `artifacts/www` (React + Vite, previewPath `/www/`), package `@workspace/www` — marketing site, tiered Stripe checkout, customer portal (license status, billing portal link, user management with roles admin/manager/staff/readonly matching desktop RBAC), shareable invite links. Backend in `artifacts/api-server` (Express, Postgres via `@workspace/db`, schema `lib/db/src/schema/licensing.ts`): cookie-session auth (scrypt), Stripe webhook (raw body before express.json) + stripe-replit-sync, license activate/verify APIs for the desktop app (contract: `docs/desktop-license-api.md`). License status derived live from synced Stripe subscription: pause at end of paid period, resume immediately; key format `RNP-XXXX-XXXX-XXXX-XXXX`, 14-day offline grace. Products seeded via `artifacts/api-server/scripts/seed-products.ts` (starter $49/3 seats, professional $99/10, enterprise $249/50; matched by product `metadata.tier`). API contract spec: `lib/api-spec/openapi.yaml` (orval codegen → `@workspace/api-client-react`, `@workspace/api-zod`).
- **Mobile companion**: Expo app `artifacts/rentnotice-field` ("RentNotice Field", previewPath `/field`) for process servers — assignment list, camera+GPS evidence capture, offline-first via AsyncStorage cache + persistent mutation queue (`context/FieldSyncContext.tsx`). Talks to the sync relay through generated hooks in `lib/api-client-react` (base URL from `EXPO_PUBLIC_DOMAIN`).
- **Sync relay (opt-in)**: `artifacts/api-server/src/routes/field.ts` + Postgres tables in `lib/db/src/schema/field.ts` (`field_assignments`, `field_evidence`). Relay-only — desktop stays offline-first; it explicitly pushes/pulls on the Field Service page (`/field-service`, note: NOT `/field`, which is the mobile artifact's proxy path). Conflict rule: last-write-wins by `updatedAt`; when mobile is newer, desktop push only refreshes the notice snapshot; evidence is append-only and idempotent by client-generated id. Contract in `lib/api-spec/openapi.yaml` (rerun codegen after edits). Desktop opt-in toggle: Settings → Mobile Field Sync (`AppSettings.syncEnabled`).
- **Tests**: `pnpm run test` inside `artifacts/rentnotice-pro` (Vitest, standalone `vitest.config.ts` — the main `vite.config.ts` requires PORT/BASE_PATH env and cannot be reused). 117 tests: `src/lib/engine/__tests__/` (classification, calculation, deadlines, validation), `src/lib/import/__tests__/` (money/date parsing, CSV pipeline, vendor presets; fixture in `fixtures/generic-ledger.csv`), `src/lib/api/__tests__/permissions.test.ts`, `src/lib/licensing/__tests__/gate.test.ts` (license grace-period gate). `pnpm run typecheck` must also stay green.

## Legal-Logic Invariants (keep tests in sync)
- 3-day notices count **court days** (day after service = day 1; weekends + CA judicial holidays skipped). 30/60-day notices count calendar days and roll forward off weekends/holidays. Built-in holiday dataset covers 2024–2030; out-of-range years are computed on demand and flagged in `DeadlineResult.warnings`.
- Fee keywords always beat the generic "rent" keyword; negative amounts are authoritatively money-in. Rent-only calculation never includes non-rent charges, never auto-applies deposits, and only carries overpayments forward when explicitly enabled.
- Rent increases >10% of the tenant's scheduled rent raise validation warning `rent_increase_over_10_percent` (Civ. Code §827(b)(2) requires 90 days' notice; the deadline rule itself still computes 30 days — deeper fix tracked as follow-up).

## User Preferences
(none recorded yet)
